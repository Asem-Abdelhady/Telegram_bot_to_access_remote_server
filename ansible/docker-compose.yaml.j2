---
version: "3"

services:
  telegram:
    build: .
    container_name: tel_app
    volumes:
      - "$HOME:/app/storage"
    environment:
      - API_ID={{ API_ID }}
      - API_HASH={{ API_HASH }}
      - BOT_TOKEN={{ BOT_TOKEN }}
      - TG_ID={{ TG_ID }}

